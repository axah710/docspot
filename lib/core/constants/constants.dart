bool isLoggedInUser = false;
// This global variable is used to track whether the user is logged in.
// It starts as false and can be set to true based on the authentication
// status of the user.

class SharedPreferencesfKeys {
  // This class contains static constant keys used for accessing data
  // in SharedPreferences.
  static const String userToken = 'userToken';
  // Defines a constant key (userToken) for storing and retrieving the
  // user token from SharedPreferences.
}
